# Secrets
.env
.env.local
.env.*.local

# Google Cloud — compte de service (ne jamais versionner)
credentials/*.json

# Local agent / IDE artifacts
.claude/

# Local data (exports, drafts, etc.) — racine du repo uniquement
/data/

# Dépendances & build
node_modules/
dist/

# OS / IDE
.DS_Store
*.log
